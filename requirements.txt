# ── Core ML ───────────────────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
xgboost>=2.0
scipy>=1.11

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-multipart>=0.0.9

# ── Deep Learning (comment out if deploying RF/XGB only — saves ~1.5GB) ──────
# tensorflow-cpu>=2.13
